# Marketplace media

Placeholder — capture these before publishing:

- `demo.gif` — Explorer of the sample workspace showing the default badges, then assigning a custom badge via the context menu (referenced from the main README).
- `icon.png` — 128×128 extension icon; add `"icon": "images/icon.png"` to package.json once it exists.
